import 'package:flutter/material.dart';

class Historical_data extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("歷史資料分析"),
    );
  }
}